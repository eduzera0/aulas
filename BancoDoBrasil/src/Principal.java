
import br.univali.prog.banco.Banco;
import javax.sound.midi.Soundbank;

/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author 1978233
 */
public class Principal {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        BancoCLI banco = new BancoCLI();
        /*
        Banco bb = new Banco();
        int conta1 = bb.criarConta(true, 500, 100);
        bb.depositar(conta1,100);
        
        int conta2 = bb.criarConta(false,100,0);
        bb.depositar(conta2, 1000);
        bb.sacar(conta2, 100);
        
        bb.transferir(conta1, conta2, 250);
        
        System.out.println(bb.imprimirExtrato(conta1));
        System.out.println(bb.imprimirExtrato(conta2));
     */   
    }
    
}
